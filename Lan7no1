#include <stdio.h>


double functionM(void);
int functionN(int n, double x);
double functionO(double x1, int n1, double x2, int n2);
double functionP(int a, int n, int b, int c);

int main() {

    int a, b, c, d, e;
    double r, s, t, u, v;

     
     functionM(); 
   // a) ถูก

    
    a = functionM();
    
    
    // b ผิด a เป็น int (ชนิดข้อมูลไม่ตรง)

    
    b = functionN(a, b);
    
    // c ผิด argument ตัวที่ 2 ต้องเป็น double 

    
    r = functionO(r, a, s, b);
    // d ✔ ถูก
   

    
    s = functionP(a, b, c, d, e);
    // e ผิด  รับ parameter 4 ตัว แต่ส่งมา 5 ตัว
    

    
    u = functionM();
    // f ✔ ถูก
    

    
    c = d + functionN(r, s);
    // g ผิด argument ตัวแรกควรเป็น int แต่ r เป็น double
    
    

    
    t = s * functionO(r, a, r, a);
    // h ถูก
    

    
    a = v + functionP(r, s, t, t);
    // i ผิด รับค่า int แต่ส่ง double มา
    

    
    functionP(functionN(a, a), s, t, t + r);
    // j ผิด รับค่าตัวแปรไม่ตรงกัน
   

    
    v = functionP(functionN(a, a), s, t, t + r);
    // k ผิด argument ไม่ตรงชนิดข้อมูล
    
    

    return 0;
}
